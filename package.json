{
  "name": "umirhack-backend",
  "private": true,
  "scripts": {
    "db:up": "docker compose up -d postgres",
    "db:down": "docker compose stop postgres",
    "containers:stop-services": "docker compose stop auth-service user-service",
    "prisma:generate": "node ./scripts/prisma-orchestrator.mjs generate",
    "prisma:migrate:dev": "node ./scripts/prisma-orchestrator.mjs migrate-dev",
    "dev:services": "concurrently -k -n USER,AUTH -c cyan,magenta \"npm --prefix ./services/user-service run start:dev\" \"npm --prefix ./services/auth-service run start:dev\"",
    "start:dev": "npm run containers:stop-services && npm run db:up && npm run prisma:generate && npm run dev:services",
    "start:prod": "docker compose up -d --build",
    "stop:prod": "docker compose down"
  },
  "devDependencies": {
    "concurrently": "^9.2.1"
  }
}
